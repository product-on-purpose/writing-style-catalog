# Resources

This directory holds build artifacts generated from the taxonomy source files.

## taxonomy.json

Generated by `python tools/build-indexes.py`. Contains all taxonomy entries indexed by axis and ID for fast lookups by the skill and the Composer app.

Run `python tools/build-indexes.py` to regenerate after modifying entries.
