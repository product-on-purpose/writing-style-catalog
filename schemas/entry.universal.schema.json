{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/product-on-purpose/writing-style-catalog/main/schemas/entry.universal.schema.json",
  "title": "Universal Entry Base",
  "description": "Shared base schema for all taxonomy entry types",
  "type": "object",
  "required": [
    "id",
    "name",
    "axis",
    "one_liner",
    "description",
    "pairs_well_with",
    "avoid_with",
    "confusable_with",
    "when_to_use",
    "when_not_to_use",
    "llm_instruction_phrasing",
    "tags",
    "review_status",
    "tells",
    "anti_patterns",
    "failure_modes"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*[a-z0-9]$",
      "description": "Slug identifier, unique within axis"
    },
    "name": {
      "type": "string",
      "description": "Human-readable display name"
    },
    "axis": {
      "type": "string",
      "enum": ["voice", "tone", "style", "format"],
      "description": "The taxonomy axis this entry belongs to. voice and tone are both Axis 1 (Voice and Tone); style is Axis 2; format is Axis 3."
    },
    "one_liner": {
      "type": "string",
      "maxLength": 200,
      "description": "1-2 sentence standalone description"
    },
    "description": {
      "type": "string",
      "description": "Full prose description, 100-500 words"
    },
    "pairs_well_with": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "List of entry IDs that compose well with this entry"
    },
    "avoid_with": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of entry IDs that conflict or produce poor results"
    },
    "confusable_with": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Entry IDs that are commonly confused with this one"
    },
    "when_to_use": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Situations where this entry is appropriate"
    },
    "when_not_to_use": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Situations where this entry is inappropriate"
    },
    "llm_instruction_phrasing": {
      "type": "string",
      "description": "Ready-to-use instruction paragraph for an LLM"
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Searchable tags"
    },
    "review_status": {
      "type": "string",
      "enum": ["draft", "reviewed", "stable", "reference-quality", "deprecated"],
      "description": "Lifecycle state of this entry. draft = initial content; reviewed = editorially checked; stable = in active use; reference-quality = exemplary; deprecated = superseded."
    },
    "deprecated_in_favor_of": {
      "type": "string",
      "description": "If deprecated, the entry ID that replaces this one"
    },
    "examples_index": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Relative paths to example files"
    },
    "tells": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 5,
      "maxItems": 7,
      "description": "ADR 0009 pedagogical bar (required since 2026-06-22, the F2 tighten-to-required step after the 60-entry backfill). 5 to 7 spottable phrases, syntactic moves, or structural markers by which a reader identifies this entry's application in the wild. Largely reformats the per-axis fields (language_patterns, markers, structural_conventions). Substance (non-empty strings) is enforced by Gate 2 in tools/validate.py."
    },
    "anti_patterns": {
      "type": "array",
      "minItems": 2,
      "maxItems": 4,
      "items": {
        "type": "object",
        "required": ["pattern", "why"],
        "properties": {
          "pattern": { "type": "string", "description": "A common misuse of this entry" },
          "why": { "type": "string", "description": "Why it counts as misapplication" }
        }
      },
      "description": "ADR 0009 pedagogical bar (required since 2026-06-22, the F2 tighten-to-required step after the 60-entry backfill). 2 to 4 misuse/why pairs; consumed by Gate 2. Makes explicit what is implicit in when_not_to_use and confusable_with."
    },
    "failure_modes": {
      "type": "array",
      "minItems": 2,
      "maxItems": 3,
      "items": {
        "type": "object",
        "required": ["mode", "mitigation"],
        "properties": {
          "mode": { "type": "string", "description": "A specific way the entry breaks in practice" },
          "mitigation": { "type": "string", "description": "How the writer recognizes and corrects it" }
        }
      },
      "description": "ADR 0009 pedagogical bar (required since 2026-06-22, the F2 tighten-to-required step after the 60-entry backfill). 2 to 3 mode/mitigation pairs. The field the C1 restraint check renders against: a sample is judged a caricature if it triggers the entry's own declared failure modes."
    }
  }
}
